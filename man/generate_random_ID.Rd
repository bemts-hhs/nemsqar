% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{generate_random_ID}
\alias{generate_random_ID}
\title{Generate Random ID}
\usage{
generate_random_ID(n, set_seed = 12345)
}
\arguments{
\item{n}{Integer. The number of random IDs to generate.}

\item{set_seed}{Numeric or \code{NULL}. The seed value for random number
generation. If a numeric value is provided, the seed is set
to ensure reproducibility. If \code{NULL}, no seed is set, and the
results will vary across executions.}
}
\value{
Returns a character vector containing \code{n} randomly generated IDs.
Each ID is formatted as a string of 10 letters followed by a hyphen
and a 10-digit number.
}
\description{
Generate a random set of letters and numbers to create a random ID.
}
\details{
The \code{generate_random_ID} function generates a specified number of random IDs.
Each ID consists of 10 randomly chosen upper/lower case letters followed by a
10-digit random number. Optionally, a seed can be set for reproducibility.
}
\section{Credit}{


This function was developed by (Nicolas Foss, Ed.D., MS)\link{nicolas.foss@hhs.iowa.gov} at the Bureau of Emergency Medical and Trauma
Services, Division of Public Health, Iowa HHS.
}

\examples{
# Generate 5 random IDs with a seed for reproducibility
random_ids <- generate_random_ID(5, set_seed = 42)
print(random_ids)

# Generate 3 random IDs without setting a seed
random_ids_no_seed <- generate_random_ID(3, set_seed = NULL)
}
