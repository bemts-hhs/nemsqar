% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare_normality.R
\name{compare_grouped_is_it_normal}
\alias{compare_grouped_is_it_normal}
\title{Compare Grouped Is It Normal}
\usage{
compare_grouped_is_it_normal(
  df,
  data_name = deparse(substitute(df)),
  x,
  group_var,
  include_plots = TRUE,
  filter1 = NULL,
  filter2 = NULL,
  operator = NULL,
  scale = FALSE,
  print_out = TRUE,
  plot_theme = theme_cleaner,
  ...
)
}
\arguments{
\item{df}{The input dataframe containing the data to be analyzed.}

\item{data_name}{(optional) The name of the dataframe. Defaults to the
expression used for the df argument.  If user-supplied,
must be a string vector of length 1.}

\item{x}{<\link[=dplyr_tidy_select]{'tidy-select'}> The name of the column containing
the numeric variable to analyze for normality.}

\item{group_var}{<\link[=dplyr_tidy_select]{'tidy-select'}> The name of the column
containing a factor variable to be used for grouping the data.}

\item{include_plots}{Logical whether to include plots in the output (defaults to TRUE)}

\item{filter1}{(optional) The first filter condition for the numeric variable,
containing an RHS logical comparator as a string (e.g., "< 50")}

\item{filter2}{(optional) The second filter condition for the numeric variable,
containing an RHS logical comparator as a string (e.g., "< 100")}

\item{operator}{(optional) the logical operator to combine filter1 and filter2.}

\item{scale}{Logical. Whether to apply min-max normalization to the numeric
variable.(default is FALSE).}

\item{print_out}{Logical. Whether to print the results to the consule. Defaults
to TRUE.}

\item{plot_theme}{A function that defines the theme for the plots. Defaults to \code{theme_cleaner}.}

\item{...}{Additional arguments passed to the plot_theme.}
}
\value{
Comparisons
}
\description{
Comparative function for one numeric vector and a factor grouping variable
}
\details{
The \code{compare_grouped_is_it_normal} function allows users to analyze the
normality of a numeric variable across groups defined by a factor variable.
It performs Shapiro-Wilk normality tests and provides descriptive statistics
and visualizations (including Q-Q plots, histograms, kernel density plots,
and boxplots). Users can apply filters, scale the data using min-max
normalization, and customize the output with plot themes.

@section Output:
\strong{Descriptive Statistics}: Summary statistics including mean, standard
deviation, minimum, quartiles, median, maximum, and missing data information
for each group.
\strong{Shapiro-Wilk Test Results}: P-values for the normality tests, along with
a diagnosis of normal or non-normal distribution.
\strong{Visualizations}: Optional plots such as Q-Q plots, histograms, kernel
density plots, and boxplots for each group.

@section features:
}
\section{Features}{
\itemize{
\item \strong{Normality Testing}: Uses the Shapiro-Wilk test for normality.
\item \strong{Filtering}: Users can filter the data based on conditions using the
filter1, filter2, and operator arguments.
\item \strong{Scaling}: Optional min-max normalization for the numeric variable.
\item \strong{Customizable Plot Themes}: Allows users to apply custom ggplot2 themes
to the output plots.
\item \strong{Comprehensive Descriptive Statistics}: Provides summary statistics
across all groups.
}
}

\examples{
df <- data.frame(
  score = rnorm(100, mean = 50, sd = 10),
  group = factor(sample(c("A", "B", "C"), 100, replace = TRUE))
)

compare_grouped_is_it_normal(df, x = score, group_var = group)

}
